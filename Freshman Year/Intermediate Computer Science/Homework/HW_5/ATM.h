class ATM{
private:
  Account accoutList[10];
public:
  ATM();

  bool isValid(int);

  void mainMenu();

  void checkBalance(nt);
  void makeWithdraw(int, int);
  void makeDeposit(int, int);
};
